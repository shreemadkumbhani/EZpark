services:
  - type: web
    name: ezpark-backend
    env: node
    plan: free
    region: oregon
    rootDir: parkeasy-backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 10000
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false